import { MarkerStore } from './marker.store'

xdescribe('MarkerStore', () => {
  let markerStore: MarkerStore

  beforeAll(() => {
    markerStore = MarkerStore.instance
  })

  it('should get next back marker', (done) => {
    done()
  })

  it('should get next forward marker', (done) => {
    done()
  })
})
